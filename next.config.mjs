/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['assets.pancakeswap.finance', 's2.coinmarketcap.com'],
  },
}

export default nextConfig
